# Мини-приложение для Telegram бота

## Что было сделано

✅ Создана структура мини-приложения с двумя экранами:
- **Экран 1 (Главный):** имя пользователя, текущая серия дней без грызения, кнопки "Записать момент" и "Статистика"
- **Экран 2 (Статистика):** график дней без грызения и количество записанных моментов

✅ Добавлены API endpoints в бота:
- `/api/user` — получение данных пользователя
- `/api/events` — получение событий и данных для графика

✅ Добавлена кнопка "📱 Мини-приложение" в меню бота

✅ Настроена авторизация через Telegram Web App API

## Что нужно сделать дальше

### 1. Обновить URL API в `webapp/app.js`

Откройте файл `webapp/app.js` и найдите строку:
```javascript
const API_URL = 'https://your-bot.railway.app';
```

Замените `https://your-bot.railway.app` на реальный URL вашего бота на Railway.

**Как узнать URL бота на Railway:**
- Зайдите в ваш проект на Railway
- В разделе "Settings" → "Networking" найдите "Public Domain"
- Скопируйте этот URL (например: `https://your-bot-name.up.railway.app`)

### 2. Развернуть мини-приложение на Vercel

**Вариант А: Через веб-интерфейс Vercel**

1. Зайдите на [vercel.com](https://vercel.com) и зарегистрируйтесь (можно через GitHub)
2. Нажмите **"Add New Project"**
3. Подключите ваш GitHub репозиторий
4. В настройках проекта:
   - **Root Directory:** укажите `webapp` (или оставьте пустым, если файлы в корне)
   - **Framework Preset:** выберите **"Other"**
   - **Build Command:** оставьте пустым
   - **Output Directory:** оставьте пустым
5. Нажмите **"Deploy"**
6. После деплоя скопируйте URL (например: `https://your-project.vercel.app`)

**Вариант Б: Через командную строку**

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Перейдите в папку с мини-приложением
cd webapp

# Запустите деплой
vercel

# Следуйте инструкциям в терминале
```

### 3. Настроить переменную окружения в Railway

1. Зайдите в ваш проект на Railway
2. Откройте вкладку **"Variables"**
3. Добавьте новую переменную:
   - **Key:** `WEBAPP_URL`
   - **Value:** URL вашего приложения на Vercel (например: `https://your-project.vercel.app`)
4. Сохраните изменения

### 4. Перезапустить бота

После добавления переменной `WEBAPP_URL`:
- Railway автоматически перезапустит бота, или
- Вы можете перезапустить вручную в разделе "Deployments"

### 5. Протестировать

1. Откройте бота в Telegram
2. Нажмите кнопку **"📱 Мини-приложение"**
3. Должно открыться мини-приложение с вашими данными

## Структура файлов

```
proj_bot/
├── main.py                    # Бот с API endpoints
├── db.py                      # База данных
├── webapp/                    # Мини-приложение
│   ├── index.html            # HTML страница
│   ├── style.css             # Стили
│   ├── app.js                # JavaScript логика
│   └── vercel.json           # Конфигурация Vercel
├── DEPLOYMENT.md             # Подробная инструкция
└── WEBAPP_README.md          # Этот файл
```

## Как это работает

1. Пользователь нажимает кнопку "📱 Мини-приложение" в боте
2. Telegram открывает веб-страницу с мини-приложением (размещенную на Vercel)
3. Мини-приложение получает `initData` от Telegram (данные пользователя)
4. Мини-приложение отправляет `initData` на API бота (на Railway)
5. Бот проверяет подлинность `initData` и возвращает данные пользователя
6. Мини-приложение отображает данные пользователя

## Возможные проблемы

### Мини-приложение не открывается
- Проверьте, что `WEBAPP_URL` установлен в Railway
- Убедитесь, что сайт доступен по HTTPS
- Проверьте URL в `app.js`

### Ошибка "Invalid auth"
- Проверьте, что `BOT_TOKEN` установлен в Railway
- Убедитесь, что код проверки `initData` работает правильно

### Данные не загружаются
- Откройте консоль браузера (F12) и проверьте ошибки
- Проверьте логи на Railway
- Убедитесь, что API endpoints доступны

## Дополнительная информация

Подробная инструкция по развертыванию находится в файле `DEPLOYMENT.md`.

## Что дальше?

После успешного развертывания вы можете:
- Изменить дизайн мини-приложения (файлы в `webapp/`)
- Добавить новые функции
- Настроить кастомный домен в Vercel

Все изменения автоматически задеплоятся при коммите в Git (если настроен автоматический деплой).
