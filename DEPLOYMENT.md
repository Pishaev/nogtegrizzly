# Инструкция по развертыванию мини-приложения на Vercel

## Что такое мини-приложение Telegram?

Мини-приложение (Web App) — это веб-приложение, которое открывается прямо внутри Telegram. Пользователи могут взаимодействовать с ним без перехода в браузер.

## Что нужно сделать

### Шаг 1: Подготовка кода

1. **Убедитесь, что все файлы на месте:**
   - `webapp/index.html` — главная страница
   - `webapp/style.css` — стили
   - `webapp/app.js` — логика приложения
   - `webapp/vercel.json` — конфигурация Vercel

2. **Обновите URL API в `webapp/app.js`:**
   - Найдите строку: `const API_URL = 'https://your-bot.railway.app';`
   - Замените на URL вашего бота на Railway (например: `https://your-bot-name.railway.app`)

### Шаг 2: Регистрация на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Зарегистрируйтесь через GitHub (рекомендуется) или email
3. Подтвердите email, если потребуется

### Шаг 3: Подключение репозитория

1. В Vercel нажмите **"Add New Project"** (или **"New Project"**)
2. Подключите ваш GitHub репозиторий (или импортируйте проект)
3. Выберите репозиторий с вашим ботом

### Шаг 4: Настройка проекта в Vercel

1. **Root Directory:** Оставьте пустым или укажите `webapp` (если Vercel не видит файлы)
2. **Framework Preset:** Выберите **"Other"** или **"Static"**
3. **Build Command:** Оставьте пустым (статический сайт)
4. **Output Directory:** Оставьте пустым или укажите `.` (текущая директория)

### Шаг 5: Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения деплоя (обычно 1-2 минуты)
3. После успешного деплоя вы получите URL вида: `https://your-project.vercel.app`

### Шаг 6: Настройка переменных окружения в Railway

1. Перейдите в ваш проект на Railway
2. Откройте **Variables** (Переменные окружения)
3. Добавьте новую переменную:
   - **Key:** `WEBAPP_URL`
   - **Value:** URL вашего приложения на Vercel (например: `https://your-project.vercel.app`)

### Шаг 7: Обновление кода бота

После добавления переменной `WEBAPP_URL` в Railway:

1. Перезапустите бота на Railway (или подождите автоматического перезапуска)
2. Кнопка "📱 Мини-приложение" появится в меню бота

### Шаг 8: Тестирование

1. Откройте бота в Telegram
2. Нажмите кнопку **"📱 Мини-приложение"**
3. Должно открыться мини-приложение с вашими данными

## Структура проекта

```
proj_bot/
├── main.py              # Основной файл бота с API endpoints
├── db.py                # Работа с базой данных
├── webapp/              # Мини-приложение
│   ├── index.html      # HTML страница
│   ├── style.css       # Стили
│   ├── app.js          # JavaScript логика
│   └── vercel.json     # Конфигурация Vercel
└── DEPLOYMENT.md       # Эта инструкция
```

## API Endpoints

Бот предоставляет два API endpoint:

1. **POST `/api/user`** — получение данных пользователя
   - Требует: `{"initData": "..."}`
   - Возвращает: `{"name": "...", "current_streak": 0, "max_streak": 0}`

2. **POST `/api/events`** — получение событий и данных для графика
   - Требует: `{"initData": "..."}`
   - Возвращает: `{"events": [...], "chartData": [...]}`

## Безопасность

- Авторизация происходит через проверку `initData` от Telegram
- Используется HMAC-SHA-256 для проверки подлинности
- Только авторизованные пользователи могут получить доступ к данным

## Возможные проблемы и решения

### Проблема: Мини-приложение не открывается

**Решение:**
- Проверьте, что URL в `WEBAPP_URL` правильный
- Убедитесь, что сайт доступен по HTTPS
- Проверьте, что в `app.js` указан правильный `API_URL`

### Проблема: Ошибка "Invalid auth"

**Решение:**
- Проверьте, что `BOT_TOKEN` установлен в Railway
- Убедитесь, что `initData` передается правильно

### Проблема: Данные не загружаются

**Решение:**
- Проверьте консоль браузера (F12) на наличие ошибок
- Убедитесь, что API endpoints доступны
- Проверьте логи на Railway

### Проблема: CORS ошибки

**Решение:**
- Vercel автоматически обрабатывает CORS для статических файлов
- Если проблемы остаются, добавьте заголовки в `vercel.json`

## Дополнительные настройки

### Кастомный домен

Если хотите использовать свой домен:

1. В Vercel перейдите в **Settings** → **Domains**
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям Vercel
4. Обновите `WEBAPP_URL` в Railway

### Обновление мини-приложения

После изменений в `webapp/`:

1. Закоммитьте изменения в Git
2. Vercel автоматически задеплоит новую версию
3. Обычно это занимает 1-2 минуты

## Полезные ссылки

- [Документация Telegram Web Apps](https://core.telegram.org/bots/webapps)
- [Документация Vercel](https://vercel.com/docs)
- [Документация Railway](https://docs.railway.app)

## Поддержка

Если возникли проблемы:
1. Проверьте логи в Vercel (Deployments → выберите деплой → View Function Logs)
2. Проверьте логи в Railway
3. Проверьте консоль браузера в мини-приложении (откройте DevTools)
